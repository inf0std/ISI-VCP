const handleLogin = (req, res, next)=>{
    const {email, pwd} = req.body;
    try{
        
    }catch(err){
        
    }
}

const handleSubscribe = (req, res, next)=>{
    
}

const handleProfileInfo = (req, res, next)=>{
    
}